# P0 상세 설계

## 목표
릴스의 오케스트레이션 워크플로우를 그대로 태우되, **한국 갭(네이버 블로그·파워링크)** 을 로컬 MCP로 채워
사장님이 대화로 셋업 → 첫 카루셀을 인스타·링크드인·네이버 블로그에 (승인 후) 게시하는 것까지 동작시킨다.

## 범위 (In)
- [x] 레포 스캐폴드 · README(2트랙) · 라이선스 · .gitignore(크리덴셜 차단)
- [x] `brand_voice.template.md` · `policy.example.yaml` · `connectors.md`
- [x] `naver-marketing-mcp` 골격: 도구 인터페이스 + 안전장치(승인 게이트·상한·지연·dry-run)
- [x] 셋업 마법사 스킬
- [ ] `connect_naver` 실제 구현 (Playwright 로그인 → 세션 키체인 저장)
- [ ] `naver_blog_publish` 실제 게시 (마크다운 → 네이버 에디터, 이미지 첨부)
- [ ] `powerlink_bidding` — naver-powerlink-bidding 통합
- [ ] `runs.db`(SQLite) 로그 + `check_daily_cap` 실제 카운트
- [ ] 사장님용 QUICKSTART 문서

## 범위 (Out — P1+)
- 성과 폐루프(collect_performance) · 스케줄러 무인화 · 전자동 opt-in · 멀티 유료채널 · 대시보드

## 핵심 리스크와 방어
| 리스크 | 방어 |
| --- | --- |
| 네이버 계정 정지 | 로컬·자기IP·단일계정 + 승인 게이트 기본 + 일일 상한 + 랜덤 지연 + 세션 재사용 |
| 크리덴셜 유출 | OS 키체인만, 세션·`.env` 커밋 차단(.gitignore), 클라우드 미전송 |
| 네이버 UI 변경으로 자동화 파손 | 셀렉터를 한 모듈(`naver_blog.py`)에 격리, 실패 시 스크린샷 남기고 초안 보존 |
| 비개발자 설치 장벽 | chat-first 셋업 마법사 + 커넥터는 OAuth 원클릭 2개 |

## 다음 착수 순서 (제안)
1. `naver_blog.py` — Playwright 세션 저장/복원 + 블로그 글쓰기 플로우
2. `runs.db` 스키마 + `check_daily_cap` 실카운트
3. naver-powerlink-bidding 의존성 통합 → `powerlink_bidding` 연결
4. 사장님 QUICKSTART + 엔드투엔드 리허설(초안까지)
